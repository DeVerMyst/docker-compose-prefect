[project]
name = "docker-compose-prefect"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "prefect>=3.6.21",
    "uvicorn>=0.41.0",
]
